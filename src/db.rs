use chrono::NaiveDate;

use crate::article::Article;

pub fn store(articles: Vec<Article>) {
    todo!()
}

// Support for a single category
pub fn fetch(date: NaiveDate) -> Vec<Article> {
    todo!()
}
